-- Requires 'pre-lazy/init.lua'
require("pre-lazy")

-- Load the lazy package manager. (after custom, keymaps for plugins, should live sperately)
require("lazy-nvim")

-- Requires 'poist-lazy/init.lua'
require("post-lazy")
